import { DefaultRootState } from "../store"

export const allIngredients = (store:DefaultRootState) => store.allIngredients
export const allIngredientsArray = (store:DefaultRootState) => store.allIngredients.ingredients